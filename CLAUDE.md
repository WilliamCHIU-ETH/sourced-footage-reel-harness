# 你在 sourced-footage-reel harness

這個資料夾不是專案原始碼，是一份**做影片的方法**。在這個 CWD 開的 session，任務只有一種：

> 讀一篇文字來源（例如財經晨報），從裡面挑出有公開官方影片可用的技術題材，取得那支素材，剪成固定版面的 9:16 直式短影音，配上自寫中文字幕與整片固定的三行大標。

**先讀 [`SKILL.md`](SKILL.md)。** 那是唯一入口，步驟、判準、失敗處置都在裡面，不要憑既有印象動手。

## 三件事先知道

1. **常態只有兩個 gate 會停下來等人**：題材與素材的挑選（GATE 1）、成片驗收（GATE 2）。另有一個條件式 gate——選鏡時可用鏡頭撐不起指定片長才觸發，因為片長是呼叫方指定的輸入、不可自行更改。到了就停。**其餘不確定一律自己判斷後繼續，理由留到下一個 gate 一併出示**——不要每遇到一件不清楚的事就停一次。完整規則見 SKILL.md 的「停頓只有三種」。
2. **所有產出寫到輸入指定的 `run_dir`，不要寫進這個資料夾。** harness 不帶狀態。
3. **`fixtures/inputs.example.json` 是一支已完成的成品**（含題材、素材識別碼、大標、20 段字幕）。它只用於對照欄位格式，**不得當作新題材的底稿或答案**。開新題材用 `fixtures/inputs.minimal.json`。同理 `RECIPE.md` 是史料，裡面刻意不留上次的題材答案。

## 檔案

| 檔案 | 作用 |
|---|---|
| `SKILL.md` | 入口：步驟序列與兩個 gate |
| `ASSUMPTIONS.md` | 環境／工具／憑證／知識假設。**有幾個不知道會走死路的限制** |
| `inputs.schema.json` | 輸入定義，以及 `$defs.candidate`（步驟 2 產物的欄位與值域） |
| `VERIFY.md` | 怎麼判定做對了 |
| `bin/` | 三支純機械 script：建專案骨架、取素材＋正規化、驗成片 |
| `fixtures/` | 最小底稿、已完成成品範例、composition 模板 |
| `RECIPE.md` | 上一次的完整軌跡與踩過的坑（史料，不是指令） |

## 要改這份 harness 的話

**再加東西前先問「不寫進去，下一個 agent 會不會做錯」——答案是不會的，就不要加。** 這條原本寫在 `RECIPE.md` 階段 I，但 RECIPE 是史料、不會每次被讀到，而煞車必須在入口。逐條判斷之外還要看總量：`SKILL.md` 與 `ASSUMPTIONS.md` 是每次必讀的熱路徑，它們的**成長速率本身就是一個指標**（階段 J：一輪 +7.8%）。

## 不做什麼

不生成任何畫面（無 AI 生圖、無生成式 B-roll）、不做主播或虛擬人、不做多支素材混剪、不做旁白配音、不做競品分析。找不到公開官方素材的題材就不做——那是邊界，不是失敗。
